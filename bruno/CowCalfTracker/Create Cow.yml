info:
  name: Create Cow
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/cattle
  body:
    type: json
    data: |-
      {
        "name": "My First Cow",
        "tagNumber": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
